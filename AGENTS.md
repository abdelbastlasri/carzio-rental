# AGENTS.md — carzio-aireport-agadir

This repository is empty. No source code, build config, or tooling has been initialized yet.

- **Project name**: carzio-aireport-agadir (inferred from directory name)
- **Status**: Blank project — no package manager, framework, or language chosen.
- **No git repo**: Initialize with `git init` before committing.
- No README, no CI, no lint/format/test config exists.

All work here starts from scratch. Choose a stack, scaffold the project, and set up tooling before writing application code.
